---
title: Select
description: A component for collecting long-form data Select.
client: true # Or true if JS is required for this component to function
links:
  {
    "storybook": "https://google.com",
    "github": "https://github.com/skyward-digital/sky-ui/tree/main/ui/components/Select",
    "api": "https://www.radix-ui.com/primitives/docs/components/select",
  }
---

import { CodePreview } from "#/src/components/Tabs"
import {
  Select,
  SelectWrapper,
} from "#/ui/components/Select"
import { CustomSelectOption } from "#/src/components/Select/CustomSelectOption"
import { employees } from "#/src/data/employees"

<CodePreview>
  <SelectWrapper label="Name" hint="Enter your full name">
    <Select options={employees.map(employee => employee.name)} client:load />
  </SelectWrapper>
</CodePreview>

{/* ## Installation */}

## Usage

```jsx
import { Select, SelectWrapper, SelectOption } from "#/ui/components/Select"
```

```jsx
<SelectWrapper label="Name">
  <Select options={employees.map(employee => employee.name)} />
</SelectWrapper>
```

## How it works

The `Select` component provides some base styling to the radix `Select` element.

The `SelectWrapper` goes around your `Select` to provide additional associated elements easily like labels & hints, but it's totally optional if you want to use a `Select` separate from your labels and roll-your-own.

<CodePreview client:load>
  <SelectWrapper label="Name">
    <Select options={employees.map(employee => employee.name)} client:load />
  </SelectWrapper>
  <div slot="code">
    ```jsx
    <SelectWrapper label="Name">
      <Select options={employees.map(employee => employee.name)} client:load />
    </SelectWrapper>
    ```
  </div>
</CodePreview>

<CodePreview client:load>
  <div className="w-full flex items-center justify-center gap-4">
    <label htmlFor="example-Select">
      Custom Label
    </label>
    <Select id="example-Select" options={employees.map(employee => employee.name)} client:load />
  </div>
  <div slot="code">
    ```jsx
    <div className="w-full flex items-center">
      <label htmlFor="example-Select" className="w-1/3">
        Custom Label
      </label>
      <Select id="example-Select" options={employees.map(employee => employee.name)} client:load />
    </div>
    ```
  </div>
</CodePreview>

## Hint

You can pass a hint to the `SelectWrapper` to provide additional context to the user.

<CodePreview client:load>
  <SelectWrapper label="Name" hint="What do you call yourself?">
    <Select options={employees.map(employee => employee.name)} client:load />
  </SelectWrapper>
  <div slot="code">
    ```jsx
    <SelectWrapper label="Name" hint="required">
      <Select options={employees.map(employee => employee.name)} client:load />
    </SelectWrapper>
    ```
  </div>
</CodePreview>

## Error

Passing an error to the `SelectWrapper` and `Select` will display error styles. If a hint exists, it will be replaced with the error.

<CodePreview client:load>
  <SelectWrapper label="Name" hint="required" error="Your name is required">
    <Select options={employees.map(employee => employee.name)} error client:load />
  </SelectWrapper>
  <div slot="code">
    ```jsx
    <SelectWrapper label="Name" hint="required" error="Your name is required">
      <Select options={employees.map(employee => employee.name)} error={true} client:load />
    </SelectWrapper>
    ```
  </div>
</CodePreview>

## Customization

### Custom Option Elements

If you need more fine grain control over your `option` elements and how they look, you can use the `SelectOption` element and pass it as a child.

The child elements of `SelectOption` will be displayed however you create them. In order to ensure the `Select` works properly, you must pass a `value` string to the `SelectOption` element.

<CodePreview client:load>
  <CustomSelectOption client:load/>
  <div slot="code">
    ```jsx
    <SelectWrapper label="Name">
      <Select>
        {employees.map((employee) => (
          <SelectOption key={employee.name} value={employee.name}>
            <div className="flex items-center gap-2 text-left">
              <img
                src={employee.avatar}
                alt={employee.name}
                className="w-8 h-8 rounded-full"
              />
              <div>
                <div className="font-bold">{employee.name}</div>
                <div className="text-xs text-gray-500">{employee.role}</div>
              </div>
            </div>
          </SelectOption>
        ))}
      </Select>
    </SelectWrapper>
    ```
  </div>
</CodePreview>