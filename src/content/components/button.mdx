---
title: Button
description: Buttons are components that allow users to take actions within your website or app.
links:
  {
    "storybook": "https://skyward-digital.github.io/sky-ui/?path=/story/ui-components-button--all",
    "github": "https://github.com/skyward-digital/sky-ui/tree/main/ui/components/Button",
  }
---

import { ArrowLeftIcon, ArrowRightIcon, Trash01Icon } from "@untitledui-icons/react/line"
import { CodePreview } from "#/src/components/Tabs"
import { Button } from "#/ui/components/Button"
import { Table, TableCell, TableHeaderCell, TableRow } from "#/ui/components/Table"

<CodePreview client:load>
  <Button variant="primary">Primary</Button>
</CodePreview>

{/* ## Installation */}

## Usage

```jsx
import { Button } from "#/ui/components/Button"
```

```jsx
<Button variant="primary">Primary</Button>
```

## Variants

The basic button comes with 4 variants

<CodePreview client:load>
  <Button variant="primary">Primary</Button>
  <Button variant="outline">Outline</Button>
  <Button variant="ghost">Ghost</Button>
  <Button variant="text">Text</Button>
  <div slot="code">
    ```jsx
    <Button
      variant="primary" // or outline, ghost, text
    >
      Primary
    </Button>
    ```
  </div>
</CodePreview>

## Size

Each button has 6 different size options to choose between

<CodePreview client:load>
  <Button variant="primary" size="xs">
    xs
  </Button>
  <Button variant="primary" size="sm">
    sm
  </Button>
  <Button variant="primary" size="md">
    md
  </Button>
  <Button variant="primary" size="lg">
    lg
  </Button>
  <Button variant="primary" size="xl">
    xl
  </Button>
  <div slot="code">
    ```jsx
    <Button
      variant="primary"
      size="xs" // or sm, md, lg, xl
    >
      Button
    </Button>
    ```
  </div>
</CodePreview>

All of these different sizes are available for each variant

<CodePreview>
  <Button variant="outline" size="xs">
    Button
  </Button>
  <Button variant="ghost" size="md">
    Button
  </Button>
  <Button variant="primary" size="xl">
    Button
  </Button>
</CodePreview>

## Destructive

You can use the `destructive` prop to indicate a destructive action. This can be combined with any variant to apply appropriate styling

<CodePreview client:load>
  <Button variant="primary" destructive>
    Primary
  </Button>
  <Button variant="outline" destructive>
    Outline
  </Button>
  <Button variant="ghost" destructive>
    Ghost
  </Button>
  <Button variant="text" destructive>
    Text
  </Button>
  <div slot="code">
    ```jsx
    <Button
      variant="primary" // or outline, ghost, text
      destructive
    >
      Primary
    </Button>
    ```
  </div>
</CodePreview>

## Icons

You can place icons within a button by passing it as a child

<CodePreview client:load>
  <Button variant="primary">
    <ArrowLeftIcon /> Previous
  </Button>
  <Button variant="primary">
    Next <ArrowRightIcon />
  </Button>
  <div slot="code">
    ```jsx
    <Button variant="primary">
      <ArrowLeftIcon /> Previous
    </Button>
    <Button variant="primary">
      Next <ArrowRightIcon />
    </Button>
    ```
  </div>
</CodePreview>

If you use an icon without text, make sure to include a hidden label for accessibility purposes

<CodePreview client:load>
  <Button variant="primary" destructive>
    <span className="sr-only">Delete</span>
    <Trash01Icon />
  </Button>
  <div slot="code">
    ```jsx
    <Button variant="primary" destructive>
      <span className="sr-only">Delete</span>
      <Trash01Icon />
    </Button>
    ```
  </div>
</CodePreview>

SVG Icons will automatically be resized based on the button size

<CodePreview client:load>
  <Button variant="primary" destructive size="xs">
    <span className="sr-only">Delete</span>
    <Trash01Icon />
  </Button>
  <Button variant="primary" destructive size="sm">
    <span className="sr-only">Delete</span>
    <Trash01Icon />
  </Button>
  <Button variant="primary" destructive size="md">
    <span className="sr-only">Delete</span>
    <Trash01Icon />
  </Button>
  <Button variant="primary" destructive size="lg">
    <span className="sr-only">Delete</span>
    <Trash01Icon />
  </Button>
  <Button variant="primary" destructive size="xl">
    <span className="sr-only">Delete</span>
    <Trash01Icon />
  </Button>
</CodePreview>

## Links

You can use buttons as links by passing an href prop

<CodePreview client:load>
  <Button variant="primary" href="#icons">
    Previous
  </Button>
  <Button variant="primary" href="#onclick">
    Next
  </Button>
  <div slot="code">
    ```jsx
    <Button variant="primary" href="/page/1">
      Previous
    </Button>
    ```
  </div>
</CodePreview>

{/* Any button/link prop can also be passed */}

## Interactivity

While buttons are often used within forms or as links, there are some instances where you may need to use them for something else.

### onClick

You can pass an `onClick` prop to a button to make it interactive, just like you can with a regular HTML button

<CodePreview client:load>
  <Button variant="primary" data-button client:load>
    Primary
  </Button>
  <Button variant="outline" data-button client:load>
    Outline
  </Button>
  <Button variant="ghost" data-button client:load>
    Ghost
  </Button>
  <Button variant="text" data-button client:load>
    Text
  </Button>
  <Button variant="primary" destructive data-button client:load>
    Destructive
  </Button>
</CodePreview>

<script>{`
  const buttons = document.querySelectorAll('[data-button]');
  buttons.forEach((button) => {
    button.addEventListener('click', () => alert('Button clicked!'))
  })
`}</script>

## Customization

### Classes

You can customize individual buttons by passing a `className` prop. If this clashes with the default styles, the `className` will take precedence.

<CodePreview client:load>
  <Button
    variant="primary"
    destructive
    className="border-amber-500 bg-amber-500 hover:bg-amber-400 focus:bg-amber-400"
  >
    Warning
  </Button>
  <div slot="code">
    ```jsx
    <Button
      variant="primary"
      destructive
      className="border-amber-500 bg-amber-500 hover:bg-amber-400 focus:bg-amber-400"
    >
      Warning
    </Button>
    ```
  </div>
</CodePreview>

Due to how `tailwind-merge` works, you won't need to worry about conflicting styles when using the `className` prop.

Make sure you override all the states to ensure a consistent experience, or you'll end up with something like this

<CodePreview>
  <Button variant="primary" destructive className="bg-amber-500">
    Warning
  </Button>
</CodePreview>

## API Reference

### Button

The `Button` component extends the `HTMLButtonElement` or `HTMLAnchorElement` interface, so you can pass any prop that you would to a regular button or link. There are a few additional props that you can pass to customize the button

<Table>
  <thead>
    <TableRow>
      <TableHeaderCell>Prop</TableHeaderCell>
      <TableHeaderCell>Type</TableHeaderCell>
      <TableHeaderCell>Default</TableHeaderCell>
    </TableRow>
  </thead>
  <tbody>
    <TableRow>
      <TableCell>variant</TableCell>
      <TableCell>primary | outline | ghost | text</TableCell>
      <TableCell>primary</TableCell>
    </TableRow>
    <TableRow>
      <TableCell>size</TableCell>
      <TableCell>xs | sm | md | lg | xl</TableCell>
      <TableCell>md</TableCell>
    </TableRow>
    <TableRow>
      <TableCell>destructive</TableCell>
      <TableCell>boolean</TableCell>
      <TableCell>false</TableCell>
    </TableRow>
    <TableRow>
      <TableCell>children</TableCell>
      <TableCell>React.ReactNode</TableCell>
      <TableCell></TableCell>
    </TableRow>
  </tbody>
</Table>
